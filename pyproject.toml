[project]
name = "comfyui-arbitrary-res-sampler"
version = "0.1.0"
description = "Experimental arbitrary-resolution hierarchical sampler nodes for ComfyUI."
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 120
target-version = "py310"
